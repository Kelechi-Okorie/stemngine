// index.js

// Access the global object created by the UMD build
// const { Clock } = STEMngine;

// const clock = new Clock();
// console.log(clock);
// console.log(STEMngine)

import { Clock } from '../../dist/engine/engine.esm.js';

const clock = new Clock();
console.log(clock);
