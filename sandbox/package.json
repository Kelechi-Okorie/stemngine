{
  "name": "sandbox",
  "version": "1.0.0",
  "description": "Sandbox for Stemngine",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rollup -c",
    "dev": "rollup -c -w",
    "build:dev": "rollup -c",
    "build:dev:watch": "rollup -c -w",
    "start": "vite"
  },
  "dependencies": {
    "@stemngine/engine": "workspace:*"
  },
  "devDependencies": {
    "tweakpane": "^4.0.5",
    "vite": "^7.1.7"
  }
}
