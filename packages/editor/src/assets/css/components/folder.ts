const textContent = `
.folder {
    background: var(--panel-2);
    color: var(--text);
    padding-left: var(--space);
}

.folder-header {
    /* padding */
}

.folder -content {
    background: var(--panel-1);
}

`;

export default textContent;
